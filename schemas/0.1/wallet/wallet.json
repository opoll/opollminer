{
    "id": "/wallet/wallet",
    "type": "object",
  
    "properties": {
      "id": {
        "type": "string",
        "description": "32 byte hexidecimal id for wallet.",
        "pattern": "^[0-9A-Fa-f]{64}$"
      },
  
      "passwordHash": {
        "type": "string",
        "description": "A hash of the password provided for this wallet.",
        "pattern": "^[0-9A-Fa-f]{64}$"
      },

      "secret": {
        "type": "string",
        "description": "A pbkdf2 secret taken from password hash.",
        "pattern": "^[0-9A-Fa-f]{64}$"
      },

      "address": {
        "type": "string",
        "description": "The one address assigned to this wallet. Wallets will only hold 1 address at a time.",
        "pattern": "^[0-9A-Fa-f]{64}$"
      },

      "publicKey": {
        "type": "string",
        "description": "Public key for this wallet's address.",
        "pattern": "^[0-9A-Fa-f]{64}$"
      },

      "privateKey": {
        "type": "string",
        "description": "Private key for this wallet's address.",
        "pattern": "^[0-9A-Fa-f]{64}$"
      }
  
    },
  
    "required": [
      "id",
      "passwordHash",
      "secret",
      "address",
      "publicKey",
      "privateKey"
    ]
  }
  