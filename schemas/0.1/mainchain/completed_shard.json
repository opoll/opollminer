{
  "id": "/mainchain/completed_shard",
  "type": "object",

  "properties": {
    "pollHash": {
      "type": "string",
      "description": "a poll hash is used to identify a single poll or survey on the network and is also used as an identifier for the poll itself",
      "pattern": "^[0-9A-Fa-f]{128}$"
    },

    "shardHash": {
      "type": "string",
      "description": "a hash of the complete and terminated shard with the highest work power at the time of incorporation"
    },

    "respondentPayments": {
      "type": "array",
      "description": "an array of respondent payment objects associated with this shard",
      "items": {
        "id": "./response_payment",
        "type": "object",
        "additionalProperties": {"$ref": "#"}
      }
    }
  },

  "required": [
    "pollHash",
    "shardHash",
    "respondentPayments"
  ]
}
